apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# クラウド向け overlay(+α フェーズで使用)。レプリカ数やイメージタグをここで差し替える。
namespace: pokecalc
resources:
  - ../../base
patches:
  - path: cronjob-import-suspend-patch.yaml
